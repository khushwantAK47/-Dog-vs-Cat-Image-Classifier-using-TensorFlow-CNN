# -Dog-vs-Cat-Image-Classifier-using-TensorFlow-CNN
This project is a Convolutional Neural Network (CNN) based image classifier built with TensorFlow/Keras that can distinguish between dogs and cats from images. The model is trained on a labeled dataset using image_dataset_from_directory, with built-in validation splitting and preprocessing.
